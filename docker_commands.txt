# Remove all containers
docker rm -f $(docker ps -aq)

docker buildx build --platform=linux/amd64 -t <image-name> .

docker run -t rf_finnhub:1.2 bash

docker tag rf_finnhub:1.1 095219402441.dkr.ecr.us-east-1.amazonaws.com/finnhub:rf_finnhub2

docker push 095219402441.dkr.ecr.us-east-1.amazonaws.com/finnhub:rf_finnhub2

# Login Command AWS CLI
aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 095219402441.dkr.ecr.us-east-1.amazonaws.com/finnhub
